# asl-fingerspelling-learning-tool

1. Install requirements and make a virtual environment (Python 3.1.2)
2. application.py for Streamlit to run in a browser window, run "streamlit run application.py" in the terminal (UI)
3. inference_post_training.py to run as a separate window on your computer (no UI)
